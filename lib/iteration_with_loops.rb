def find_min_in_nested_arrays(src)
  min_temps = []
  row_count = 0 
  while row_count < src.length do
    index_count = 0
    min_temp = src[index_coun
    while index_count < src.length[row_count] do
      if min_temp
      
end